# Przykładowe repozytorium
# Zmiana z mastera
# zmiany w pliku
# Nowa linia z mastera
@ Zmiana na hotfixie
dodaje kolejne linie z przegladarki
dodaje kolejne linie z przegladarki
dodaje kolejne linie z przegladarki
dodaje kolejne linie z przegladarki

